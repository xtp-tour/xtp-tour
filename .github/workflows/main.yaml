name: Main CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  backend:
    uses: xtp-tour/xtp-tour/.github/workflows/backend.yaml
  
  frontend:
    uses: xtp-tour/xtp-tour/.github/workflows/frontend.yaml
    