# Development defaults (pnpm dev). Empty = use Vite proxy to /api.
VITE_API_BASE_URL=
# Optional: set in .env.development.local for real auth
VITE_CLERK_PUBLISHABLE_KEY=
# Optional: set in .env.development.local to enable analytics in dev
VITE_GA_MEASUREMENT_ID=
VITE_APP_VERSION=dev
